{% extends 'base.html.twig' %}

{% block title %}
    {% if current_route_name == 'tasks_mine' %}
        Mis tareas
    {% else %}
        Todas las tareas
    {% endif %}
{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h2>{% if current_route_name == 'tasks_mine' %}
            Mis tareas
        {% else %}
            Todas las tareas
        {% endif %} ✅</h2>

    {{ include('includes/tasks-list.html.twig') }}
</div>
{% endblock %}
