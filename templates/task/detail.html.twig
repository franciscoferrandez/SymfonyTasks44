{% extends 'base.html.twig' %}

{% block title %}Tarea &quot;{{task.title}}&quot;{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h2>{{task.title}} ✅</h2>
    <p class="data-task">
        {{ task.user.surname }}, {{ task.user.name }} @ {{ task.createdAt|date('d/m/Y h:i') }} | {{ task.priority }} | {{ task.hours }}h.
    </p>
    <p>{{ task.content }}</p>
</div>
{% endblock %}
