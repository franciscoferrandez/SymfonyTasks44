{% extends 'base.html.twig' %}

{% block title %}
    {% if current_route_name == 'user_edit' %}
        Perfil de usuario
    {% elseif current_route_name == 'user_password_change' %}
        Cambio de contraseña
    {% else %}
        Registro de usuarios
    {% endif %}
{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h2>
        {% if current_route_name == 'user_edit' %}
            Perfil de usuario
        {% elseif current_route_name == 'user_password_change' %}
            Cambio de contraseña
        {% else %}
            Registro de usuarios
        {% endif %}
        ✅</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}
