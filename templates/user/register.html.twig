{% extends 'base.html.twig' %}

{% block title %}
    {% if edit is defined %}
        Perfil de usuario
    {% else %}
        Registro de usuarios
    {% endif %}
{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h2>
        {% if edit is defined %}
            Perfil de usuario
        {% else %}
            Registro de usuarios
        {% endif %}
        ✅</h2>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}
