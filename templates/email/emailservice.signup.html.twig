<h1>Welcome <!-- {{ email.toName }}--> {{ user.name }}!</h1>

<p>
    You signed up as {{ user.name }} {{ user.surname }} the following email:
</p>
<p><code>{{user.email}}<!-- {{ email.to[0].address }} --></code></p>

<p>
    <a href="{{ absolute_url(path("tasks")) }}">Click here to access the app, and activate the account</a>
    (this link is valid until {{ expiration_date|date('F jS') }})
</p>
